import 'package:flutter/material.dart';

class AddLombaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Add Lomba Screen'),
      ),
    );
  }
}
